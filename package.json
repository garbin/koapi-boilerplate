{
  "name": "koapi-boilerplate",
  "version": "0.1.0",
  "description": "RESTful API framework based on koajs",
  "main": "build/app.js",
  "babel": {
    "presets": [
      "es2015",
      "stage-0"
    ]
  },
  "scripts": {
    "start": "export NODE_ENV=production && npm run build && node build/app.js",
    "staging": "export NODE_ENV=staging && npm run build && node build/app.js",
    "production": "export NODE_ENV=production && npm run build && node build/app.js",
    "test": "export NODE_ENV=test && npm run db:rollback && npm run db:setup && make test",
    "build": "babel -d build/ src/",
    "cli": "node cli.js",
    "scheduler": "node scheduler.js",
    "babel": "babel-node",
    "knex": "knex",
    "apidoc": "gulp apidoc",
    "test:coverage": "export NODE_ENV=test && npm run db:rollback && npm run db:setup && make test-cover",
    "dev:server": "NODE_ENV=development nodemon -L -x ./node_modules/.bin/babel-node src/app.es",
    "dev:apidoc": "gulp apidoc:watch",
    "serve:apidoc": "make apidoc-server",
    "serve:coverage": "make coverage-server",
    "db:rollback": "knex migrate:rollback",
    "db:setup": "knex migrate:latest && knex seed:run",
    "log": "bunyan",
    "debug:log": "bunyan ./storage/logs/debug.log"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/garbin/koapi-boilerplate.git"
  },
  "keywords": [
    "RESTful",
    "API",
    "Framework",
    "koa"
  ],
  "author": "Garbin Huang",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/garbin/koapi-boilerplate/issues"
  },
  "homepage": "https://github.com/garbin/koapi-boilerplate#readme",
  "dependencies": {
    "config": "^1.19.0",
    "cron": "^1.1.0",
    "http-server": "^0.9.0",
    "koapi": "^0.2.*",
    "moment": "^2.12.0",
    "mysql": "^2.10.2",
    "require-all": "^2.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "gulp": "^3.9.1",
    "gulp-apidoc": "^0.2.3",
    "istanbul": "^1.0.0-alpha",
    "mocha": "^2.4.5",
    "nodemon": "^1.9.1",
    "should": "^8.2.2",
    "should-http": "0.0.4",
    "supertest": "^1.2.0"
  }
}
